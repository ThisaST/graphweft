# vsce walks node_modules with an npm-style resolver when packaging the .vsix.
# pnpm's default symlinked layout breaks that walk ("missing dependency" errors),
# so use a hoisted (flat) node_modules that both pnpm and vsce/npm can resolve.
node-linker=hoisted
verify-deps-before-run=false
